<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <title>Hasil Diagnosa - NeuroDetect</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg-primary: #0a0e1a;
      --bg-secondary: #0f1724;
      --text-primary: #ffffff;
      --text-secondary: #94a3b8;
      --accent: #3B82F6;
      --accent-hover: #2563eb;
      --accent-purple: #8b5cf6;
      --card-bg: rgba(15, 23, 36, 0.6);
      --border: rgba(148, 163, 184, 0.1);
      --success: #10b981;
      --warning: #f59e0b;
      --danger: #ef4444;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    
    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: var(--bg-primary);
      color: var(--text-primary);
      overflow-x: hidden;
      line-height: 1.6;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    body::before {
      content: '';
      position: fixed;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.15) 0%, transparent 50%),
                  radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.15) 0%, transparent 50%);
      animation: gradientShift 20s ease infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes gradientShift {
      0%, 100% { transform: translate(0, 0) rotate(0deg); }
      33% { transform: translate(5%, 5%) rotate(120deg); }
      66% { transform: translate(-5%, 5%) rotate(240deg); }
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 0 24px;
      position: relative;
      z-index: 1;
      width: 100%;
      flex: 1;
    }

    header {
      position: sticky;
      top: 0;
      z-index: 1000;
      background: rgba(10, 14, 26, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
      padding: 16px 0;
      margin-bottom: 40px;
    }

    .header-content {
      max-width: 1000px;
      margin: 0 auto;
      padding: 0 24px;
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .btn-back {
      padding: 10px 18px;
      border: 1px solid var(--border);
      background: transparent;
      color: var(--text-secondary);
      border-radius: 10px;
      cursor: pointer;
      font-weight: 600;
      font-family: 'Inter', sans-serif;
      font-size: 14px;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
    }

    .btn-back:hover {
      background: rgba(255, 255, 255, 0.05);
      color: var(--text-primary);
      border-color: rgba(148, 163, 184, 0.3);
      transform: translateX(-4px);
    }

    .logo {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--accent), var(--accent-purple));
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
      overflow: hidden; /* tambahkan ini */
    }

    .logo img {
      width: 100%;
      height: 100%;
      object-fit: cover; /* agar gambar fit dengan baik */
    }

    .brand-text {
      font-size: 20px;
      font-weight: 800;
      color: var(--text-primary);
    }

    main {
      padding: 40px 0 80px;
    }

    .page-header {
      text-align: center;
      margin-bottom: 48px;
      animation: fadeInUp 0.6s ease;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .success-icon {
      width: 80px;
      height: 80px;
      margin: 0 auto 24px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--success), #059669);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 40px;
      box-shadow: 0 8px 24px rgba(16, 185, 129, 0.3);
      animation: scaleIn 0.5s ease;
    }

    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .page-title {
      font-size: 36px;
      font-weight: 900;
      margin-bottom: 12px;
      background: linear-gradient(135deg, #ffffff 0%, #94a3b8 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .page-subtitle {
      font-size: 16px;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .result-card {
      background: var(--card-bg);
      backdrop-filter: blur(20px);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 40px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.6s ease 0.2s backwards;
      margin-bottom: 24px;
    }

    .result-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--accent), var(--accent-purple));
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-bottom: 32px;
    }

    .info-item {
      background: rgba(15, 23, 36, 0.4);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 16px;
    }

    .info-label {
      font-size: 12px;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .info-value {
      font-size: 18px;
      font-weight: 700;
      color: var(--text-primary);
    }

    .disease-card {
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
      border: 1px solid rgba(59, 130, 246, 0.3);
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 32px;
    }

    .disease-name {
      font-size: 28px;
      font-weight: 800;
      color: var(--text-primary);
      margin-bottom: 8px;
    }

    .disease-code {
      display: inline-block;
      padding: 4px 12px;
      background: rgba(59, 130, 246, 0.2);
      border: 1px solid rgba(59, 130, 246, 0.4);
      color: var(--accent);
      border-radius: 8px;
      font-size: 13px;
      font-weight: 700;
    }

    .confidence-section {
      background: rgba(15, 23, 36, 0.4);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 32px;
    }

    .confidence-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 16px;
    }

    .confidence-label {
      font-size: 14px;
      color: var(--text-secondary);
      font-weight: 600;
    }

    .confidence-value {
      font-size: 32px;
      font-weight: 900;
      background: linear-gradient(135deg, var(--accent), var(--accent-purple));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .progress-bar {
      height: 12px;
      background: rgba(15, 23, 36, 0.6);
      border-radius: 100px;
      overflow: hidden;
      margin-bottom: 16px;
    }

    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent), var(--accent-purple));
      border-radius: 100px;
      transition: width 1s ease;
      box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
    }

    .confidence-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 16px;
      border-radius: 10px;
      font-weight: 700;
      font-size: 14px;
    }

    .badge-very-high {
      background: rgba(16, 185, 129, 0.15);
      border: 1px solid rgba(16, 185, 129, 0.3);
      color: var(--success);
    }

    .badge-high {
      background: rgba(34, 197, 94, 0.15);
      border: 1px solid rgba(34, 197, 94, 0.3);
      color: #22c55e;
    }

    .badge-medium {
      background: rgba(245, 158, 11, 0.15);
      border: 1px solid rgba(245, 158, 11, 0.3);
      color: var(--warning);
    }

    .badge-low {
      background: rgba(239, 68, 68, 0.15);
      border: 1px solid rgba(239, 68, 68, 0.3);
      color: var(--danger);
    }

    .badge-very-low, .badge-negative {
      background: rgba(156, 163, 175, 0.15);
      border: 1px solid rgba(156, 163, 175, 0.3);
      color: #9ca3af;
    }

    .cf-details {
      display: flex;
      gap: 16px;
      font-size: 13px;
      color: var(--text-secondary);
    }

    .section-card {
      background: rgba(15, 23, 36, 0.4);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 24px;
    }

    .section-title {
      font-size: 18px;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 16px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .section-content {
      color: var(--text-secondary);
      line-height: 1.8;
      font-size: 15px;
    }

    .symptoms-list {
      list-style: none;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 12px;
    }

    .symptom-item {
      background: rgba(15, 23, 36, 0.6);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      gap: 12px;
      transition: all 0.3s ease;
    }

    .symptom-item:hover {
      background: rgba(15, 23, 36, 0.8);
      border-color: rgba(59, 130, 246, 0.3);
      transform: translateX(4px);
    }

    .symptom-icon {
      width: 32px;
      height: 32px;
      border-radius: 8px;
      background: linear-gradient(135deg, var(--accent), var(--accent-purple));
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      flex-shrink: 0;
    }

    .symptom-text {
      flex: 1;
    }

    .symptom-code {
      display: block;
      font-size: 11px;
      color: var(--accent);
      font-weight: 700;
      margin-bottom: 2px;
    }

    .symptom-name {
      font-size: 13px;
      color: var(--text-primary);
      font-weight: 500;
    }

    .action-buttons {
      display: flex;
      gap: 12px;
      justify-content: center;
      margin-top: 40px;
      padding-top: 32px;
      border-top: 1px solid var(--border);
    }

    .btn {
      padding: 14px 28px;
      border-radius: 10px;
      font-weight: 700;
      font-size: 15px;
      transition: all 0.3s ease;
      cursor: pointer;
      border: none;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Inter', sans-serif;
      text-decoration: none;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--accent), var(--accent-purple));
      color: white;
      box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
      position: relative;
      overflow: hidden;
    }

    .btn-primary::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s ease;
    }

    .btn-primary:hover::before {
      left: 100%;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 32px rgba(59, 130, 246, 0.4);
    }

    .btn-secondary {
      background: transparent;
      color: var(--text-secondary);
      border: 1px solid var(--border);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.05);
      color: var(--text-primary);
      border-color: rgba(148, 163, 184, 0.3);
      transform: translateY(-2px);
    }

    .alert-info {
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.3);
      border-radius: 12px;
      padding: 16px 20px;
      display: flex;
      align-items: flex-start;
      gap: 12px;
      margin-top: 24px;
    }

    .alert-icon {
      font-size: 20px;
      flex-shrink: 0;
    }

    .alert-text {
      font-size: 14px;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    @media (max-width: 768px) {
      .page-title {
        font-size: 28px;
      }

      .result-card {
        padding: 24px;
      }

      .info-grid {
        grid-template-columns: 1fr;
      }

      .symptoms-list {
        grid-template-columns: 1fr;
      }

      .action-buttons {
        flex-direction: column;
      }

      .btn {
        width: 100%;
        justify-content: center;
      }

      .confidence-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 12px;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      * {
        animation: none !important;
        transition: none !important;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="header-content">
      <a href="/dashboard-user" class="btn-back">
        ← Dashboard
      </a>
      <div class="logo">
        <img src="/images/logo_nd.jpg" alt="NeuroDetect Logo">
      </div>
      <h3 class="brand-text">NeuroDetect</h3>
    </div>
  </header>

  <div class="container">
    <main>
      <div class="page-header">
        <div class="success-icon">✓</div>
        <h1 class="page-title">Hasil Diagnosa</h1>
        <p class="page-subtitle">Berikut adalah hasil analisis berdasarkan gejala yang Anda pilih</p>
      </div>

      <div class="result-card">
        <!-- Info Grid -->
        <div class="info-grid">
          <div class="info-item">
            <div class="info-label">Tanggal Diagnosa</div>
            <div class="info-value"><%= hasil.tanggal %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Metode Analisis</div>
            <div class="info-value">Forward Chaining + CF</div>
          </div>
        </div>

        <!-- Disease Card -->
        <div class="disease-card">
          <div class="disease-name"><%= hasil.penyakit.nama_penyakit %></div>
          <span class="disease-code"><%= hasil.penyakit.id_penyakit %></span>
        </div>

        <!-- Confidence Section -->
        <div class="confidence-section">
          <div class="confidence-header">
            <div>
              <div class="confidence-label">Tingkat Keyakinan</div>
              <div class="confidence-value"><%= cfPercent %>%</div>
            </div>
            <% 
              const nilaiCfNum = Number(hasil.nilai_cf) || 0;
              let badgeClass = '';
              if (cfLabel.startsWith('Negatif')) badgeClass = 'badge-negative';
              else if (cfPercent >= 80) badgeClass = 'badge-very-high';
              else if (cfPercent >= 60) badgeClass = 'badge-high';
              else if (cfPercent >= 40) badgeClass = 'badge-medium';
              else if (cfPercent >= 20) badgeClass = 'badge-low';
              else badgeClass = 'badge-very-low';
            %>
            <div class="confidence-badge <%= badgeClass %>">
              <%= cfLabel %>
            </div>
          </div>

          <div class="progress-bar">
            <div class="progress-fill" style="width: <%= cfPercent %>%"></div>
          </div>

          <div class="cf-details">
            <div>Nilai CF: <%= nilaiCfNum.toFixed(4) %></div>
            <div>•</div>
            <div>Metode: Certainty Factor</div>
          </div>
        </div>

        <!-- Description Section -->
        <div class="section-card">
          <h3 class="section-title">
            <span>📋</span>
            Deskripsi Penyakit
          </h3>
          <div class="section-content">
            <%= hasil.penyakit.deskripsi || 'Tidak ada deskripsi tersedia.' %>
          </div>
        </div>

        <!-- Solution Section -->
        <div class="section-card">
          <h3 class="section-title">
            <span>💊</span>
            Solusi & Rekomendasi
          </h3>
          <div class="section-content">
            <%= hasil.penyakit.solusi || 'Tidak ada rekomendasi tersedia.' %>
          </div>
        </div>

        <!-- Additional Notes -->
        <% if (hasil.keterangan && hasil.keterangan !== '-') { %>
        <div class="section-card">
          <h3 class="section-title">
            <span>📝</span>
            Keterangan Tambahan
          </h3>
          <div class="section-content">
            <%= hasil.keterangan %>
          </div>
        </div>
        <% } %>
      </div>

      <!-- Symptoms Section -->
      <div class="result-card">
        <h3 class="section-title">
          <span>🩺</span>
          Gejala yang Dipilih (<%= details && details.length > 0 ? details.length : 0 %>)
        </h3>

        <% if (!details || details.length === 0) { %>
          <div class="section-content">Tidak ada gejala tercatat.</div>
        <% } else { %>
          <ul class="symptoms-list">
            <% details.forEach(d => { %>
              <li class="symptom-item">
                <div class="symptom-icon">✓</div>
                <div class="symptom-text">
                  <span class="symptom-code"><%= d.id_gejala %></span>
                  <span class="symptom-name"><%= d.nama_gejala %></span>
                </div>
              </li>
            <% }) %>
          </ul>
        <% } %>

        <div class="alert-info">
          <div class="alert-icon">ℹ️</div>
          <div class="alert-text">
            <strong>Catatan:</strong> Hasil diagnosa ini bersifat prediktif dan tidak menggantikan diagnosis medis profesional. Silakan konsultasikan dengan dokter untuk pemeriksaan lebih lanjut.
          </div>
        </div>
      </div>

      <!-- Action Buttons -->
      <div class="action-buttons">
        <a href="/diagnosa" class="btn btn-secondary">Diagnosa Ulang</a>
        <a href="/riwayat" class="btn btn-primary">Lihat Riwayat →</a>
      </div>
    </main>
  </div>

  <script>
    // Animate progress bar on load
    window.addEventListener('load', () => {
      const progressFill = document.querySelector('.progress-fill');
      if (progressFill) {
        const targetWidth = progressFill.style.width;
        progressFill.style.width = '0%';
        setTimeout(() => {
          progressFill.style.width = targetWidth;
        }, 300);
      }
    });

    // Add print functionality (optional)
    function printResult() {
      window.print();
    }
  </script>
</body>
</html>